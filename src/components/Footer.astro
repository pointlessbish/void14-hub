<footer class="system-footer">
    <div class="footer-capsule">
        <div class="credit-group">
            &copy; 2026 VOID14.WTF <span class="dim">built by</span> <span class="author">pointlessbish</span>
        </div>
    </div>
</footer>

<style>
    .system-footer {
        position: fixed;
        bottom: 20px;
        left: 0;
        right: 0;
        display: flex;
        justify-content: center;
        z-index: 12000;
        pointer-events: none;
    }

    .footer-capsule {
        pointer-events: auto;
        background: rgba(7, 3, 13, 0.95);
        border: 1px solid rgba(0, 242, 255, 0.2);
        backdrop-filter: blur(12px);
        padding: 8px 24px;
        border-radius: 30px;
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.6);
    }

    .credit-group {
        font-size: 10px;
        color: rgba(255, 255, 255, 0.3);
        letter-spacing: 1px;
    }

    .author { color: #00f2ff; font-weight: 700; }
    .dim { opacity: 0.5; font-size: 9px; margin: 0 4px; }
</style>